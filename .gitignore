# --- python / build artifacts ---
__pycache__/
*.pyc
.pytest_cache/
build/
db/*.db

# --- virtualenvs ---
.venv/
.venv-rag/
.venv-imdb/
.venv-er/

# --- OS / editor cruft ---
.DS_Store
**/.DS_Store
.cache/

# --- machine-extracted drafts (regenerated by ingesters) ---
data/staged/

# --- all raw provenance kept LOCAL ONLY (minimal-repo policy) ---
# data/curated/ (source of truth) IS committed; raw is not. Includes:
#   - data/raw/imdb*  : NON-REDISTRIBUTABLE IMDb data (personal/non-commercial license)
#   - data/raw/{site,kvikmyndir,html} : heavy re-fetchable mirrors (make fetch / make kvik)
#   - data/raw/{uthlutanir,manual,wikipedia,producers} : provenance — re-supply locally to rebuild
data/raw/
