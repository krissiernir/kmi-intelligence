# Dashboard deps (the .venv used by `make run`). The core pipeline (compile/packs/ingest) is
# stdlib-only; RAG lives in .venv-rag and the IMDb enricher in .venv-imdb (see Makefile).
streamlit>=1.35.0
pandas>=2.2.0
plotly>=5          # viz.py cockpit charts
networkx>=3        # collaborator-network layout
# openai>=1.40    # OPTIONAL: app/ask_biomonsi.py text-to-SQL (+ OPENAI_API_KEY in .env)
# OPTIONAL viz backends (the "Stíll ▾" dropdown shows them only when installed):
streamlit-echarts   # prettier sankey / force graph
streamlit-agraph    # draggable collaborator network
streamlit-aggrid    # Excel-grade browse grid
